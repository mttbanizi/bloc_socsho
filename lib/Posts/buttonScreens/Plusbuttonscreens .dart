import 'package:flutter/material.dart';

class PlusButtonIconScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: new Center(
        child: new Text('PlusButtonIconScreens'),
      ),
    );
  }
}

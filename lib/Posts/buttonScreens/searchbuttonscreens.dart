import 'package:flutter/material.dart';

class SearchButtonIconScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: new Center(
        child: new Text('SearchButtonIconScreens'),
      ),
    );
  }
}
